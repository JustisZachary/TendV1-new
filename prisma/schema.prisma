generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Supabase: Project Settings → Database → Connection string → URI (Transaction pooler, port 6543).
  // Set DATABASE_URL in .env.local and in Vercel Environment Variables.
  url      = env("DATABASE_URL")
}

model Submission {
  id               String   @id @default(cuid())
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  firstName        String
  lastName         String
  email            String
  phoneType        String
  phoneNumber      String
  consentToText    Boolean
  birthdate        DateTime
  helpTopic        String
  primaryCampus    String
  regularAttender  Boolean
  additionalDetails String?
  tags             String   @default("")
}
