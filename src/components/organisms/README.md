Organisms are composed sections of a UI.
Examples: Header, Sidebar, ProductGrid.

Guidelines:
- Can use app state and data passed from pages/templates.
- Keep them focused on a section, not the whole page.
